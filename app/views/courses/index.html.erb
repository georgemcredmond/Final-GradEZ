<%= @message %>
<table>
  <tr>
    <th>Name</th>
  </tr>
  <% for .name in @courses %>
    <tr>
      <td><%= link_to course.name, course %></td>
      <td><%= link_to "Edit", edit_courset_path(course) %></td>
      <td><%= link_to "Destroy", course, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Course", new_course_path %></p>